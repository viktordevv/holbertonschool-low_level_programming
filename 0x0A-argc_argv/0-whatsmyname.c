#include <stdio.h>
/**
 * main - this is funtion main
 * @argc: this is argc
 * @argv: this is argv
 * Return: 0
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
