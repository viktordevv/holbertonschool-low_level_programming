#include <stdio.h>
/**
 * main - this is funtion main
 * @argc: this is argc
 * @argv: this is argv
 * Return: Always 0
 */
int main(int argc, char __attribute__((unused)) * argv[])
{
 printf("%d\n", --argc);
 return (0);
}
