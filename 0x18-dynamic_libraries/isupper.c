#include "main.h"
#include <stdio.h>

/**
 * _isupper - the funtion is the print
 * @c: this is the variable
 * Return: Always 0.
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
	return (0);
	}
}
