#include <stdio.h>
/**
 * main - imprime del 1 al 10
 * go to the 1 -10
 * Return: 0 for ever return
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	printf("%d", n);
	printf("\n");

	return (0);
}
