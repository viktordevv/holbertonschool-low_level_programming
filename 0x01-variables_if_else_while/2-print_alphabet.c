#include <stdio.h>
/**
 * main - Desde la a hasta la z 
 * vamos con toda
 * Return: 0 no retorna nada yayayajuuuu
 */
int main(void)
{
char f;
for (f = 'a'; f <= 'z'; f++)
putchar(f);
putchar('\n');
return (0);
}
