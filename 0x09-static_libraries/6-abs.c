#include "main.h"
/**
 * _abs - this is like main
 * @n: absolutly is heree
 * Return: like zero
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	return (0);
}
